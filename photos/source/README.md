# Private photo staging area

Original photographs are intentionally excluded from the public Git repository.

For a local gallery rebuild, copy the private originals into category directories under this folder, run `python3 tools/build_gallery.py`, and commit only the privacy-safe output in `photos/generated/` together with `gallery-data.js`.

Do not force-add the source photographs to Git.
